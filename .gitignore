# Dependencies
node_modules/

# Environment
.env
.env.*
!.env.example
!.env.pricing.example

# Ruflo runtime data (regenerated by ruflo init)
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/neural/
.claude/memory.db

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build
dist/
build/
target/

# Ruflo runtime state & vector DBs (regenerated, not source)
.swarm/
ruvector.db
agentdb.rvf
agentdb.rvf.lock

# Scratch / analysis temp files
.tmp_analysis_*.txt


# #168: the flappy-demo first-layer gate config (SHA-256 hash of the demo password);
# generated at deploy time from the out-of-band FLAPPY_DEMO_PASSWORD, never committed.
examples/flappy-demo/gate.json

# #171: optional local crew-bridge endpoint override (not committed)
examples/flappy-demo/crew.json

# ct-agent-wasm build output (generated by scripts/build-ct-agent-wasm.sh or
# docker/Dockerfile's wasm-builder stage, embedded via crates/control-plane/build.rs) --
# a build artifact, not source, same as target/.
crates/control-plane/wasm-pkg/

# Temporaere Worktrees isolierter Agenten -- nie Teil des Repos.
.claude/worktrees/

# Nebenprodukt des Skript-Imports in scripts/ (policy-predicate-guard.py laedt
# reachability-survey.py als Modul)
__pycache__/
